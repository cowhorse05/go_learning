version: "3.8"

services:
  app:
    build: ./app
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis

  redis:
    image: docker.io/library/redis:7-alpine

  nginx:
    build: ./nginx
    ports:
      - "8081:80"
    depends_on:
      - app
